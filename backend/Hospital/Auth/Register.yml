info:
  name: Register
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/auth/register"
  body:
    type: json
    data: |-
      {
          "email": "tesss2t@example.com",
          "password": "password123",
          "full_name": "Test User",
          "dni": "12341127",
          "role": "admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
