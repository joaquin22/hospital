info:
  name: List Doctors
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/doctors"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
