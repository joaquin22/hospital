info:
  name: Create Specialty
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/specialty"
  body:
    type: json
    data: |-
      {
        "name":"Test",
        "description":"Test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
