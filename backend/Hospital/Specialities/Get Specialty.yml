info:
  name: Get Specialty
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/specialty/:id"
  params:
    - name: id
      value: "2"
      type: path
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
