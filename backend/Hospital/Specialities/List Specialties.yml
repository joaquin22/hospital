info:
  name: List Specialties
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/specialty"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
