info:
  name: Patch Specialty
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{url}}/specialty"
  body:
    type: json
    data: |-
      {
        "name":"Test",
        "description":"Test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
