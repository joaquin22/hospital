info:
  name: Update Specialty
  type: http
  seq: 4

http:
  method: PUT
  url: "{{url}}/specialty/:id"
  params:
    - name: id
      value: "11"
      type: path
  body:
    type: json
    data: |-
      {
        "name":"Test update",
        "description":"Test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
