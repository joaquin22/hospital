info:
  name: Get User
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/users/:id"
  params:
    - name: id
      value: "3"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
