info:
  name: Update User
  type: http
  seq: 3

http:
  method: PUT
  url: "{{url}}/users/:id"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "email": "admin@example.com",
        "password": "password",
        "first_name": "Test update",
        "last_name": "User",
        "dni": "12341327",
        "role": "admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
